let nombre = "juan";
let edad = "25";
let apellido = "munera";

function suma (a,b) {
    let operacion = a + b;
    return operacion;
}

console.log "el resultado de la suma es: " + sumar (numero, edad);
let Resultadosuma = sumar(numero, edad);

console.log ("nobre completo y apellido " + nombre + "" + apellido + ", edad: "+ edad);
console.log ("suma de la edad y el numero" + Resultadosuma);
